import React from "react";
// import { TextInput, TextInputProps } from "react-native";
// import styled from "styled-components";
// import { theme, typographys } from "./theme";
// import { useHover } from "./useHover";

// export type InputProps = TextInputProps & {
//   hasError?: boolean;
// }
 
export const Input = (
  // props: InputProps
  ) => {
  // const [inputRef, isHovered] = useHover();

  return (
    <p>ok</p>
    // @ts-ignore
    // <div ref={inputRef}>
      // <TextField
      //   error={props.hasError}
      //   placeholder={props.placeholder}
      //   isHovered={false}
      // />
    // {/* </div> */}
  );
}

// const getBorderColor = (props: any) => {
//   switch (true) {
//     case !!props.error:
//       return theme.pallette.light.danger;
//     case props.isHovered:
//       return theme.pallette.light.secondary.main;
//     default:
//       return theme.pallette.light.greyLight;
//   }
// };

// const TextField = styled(TextInput)`
//   ${typographys.regular}
//   padding: 8px;
//   border-radius: 4px;
//   border-width: 2px;
//   width: 100%;
//   border-color: ${props => getBorderColor(props)};
// `;
